{
  "name": "puppeteer-scraper",
  "version": "1.0.0",
  "description": "A simple scraper using Playwright",
  "main": "scrape.js",
  "scripts": {
    "start": "node scrape.js",
    "postinstall": "playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.1"
  }
}
